[package]
name = "ais-crypto"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Minimal deterministic cryptographic primitives for AIS-Core."

[dependencies]
ed25519-dalek = "2"
rand_core = { version = "0.6", features = ["getrandom"] }
sha3 = "0.10"
thiserror = "2"
