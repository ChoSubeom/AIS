[package]
name = "ais-cert"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Minimal deterministic AI Certificate support for AIS-Core."

[dependencies]
ais-crypto = { path = "../ais-crypto" }
ciborium = "0.2"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
